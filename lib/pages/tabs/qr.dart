import 'package:flutter/material.dart';

class QR extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Scan QR page', style: TextStyle(fontSize: 40)),
    );
  }
}
